#include <stdio.h>

/* write a program to copy its input to its output, 
   replacing each string of one or more blanks by a single blank */

main() {

  int c, isSpace;
  c = isSpace = 0;

  while ((c = getchar()) != EOF) {
    get char
    if !Space 
      put char
    else if (flag off)
      putchar 
      switch flag
    else /*the flag is off */
      ;
  }
}